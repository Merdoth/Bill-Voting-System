import React from 'react';
/**
 *
 * @desc the functional component returns the footer if the page
 * @returns { void }
 */
const Home = () => {
  return (
    <div>
    this is a test
    </div>
  );
};

export default Home;
